<%= link_to event, class: event.category.identifier do %>
  <div class="date-time">
    <p><%= event.starts.strftime('%A') %></p>
    <p><%= event.starts.strftime('%H:%M') %></p>
  </div>
  <h1><%= event.title %></h1>
  <div class="description"><%= event.description %></div>
<% end %>
