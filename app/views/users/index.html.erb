
<h1 class="title">Mein Profil</h1>

<div class="col">
  <div class="user-img">
    <div class="image"><%= image_tag "_profil4.jpg", height: '80', width: '80', alt: 'Bild von Raffael'%>
    </div>
  </div>
  <div class="user-desc">
    <a href="#">Profil bearbeiten<i class="fa fa-pencil-square-o"></i></a>
  </div>
</div>



<h1 class="title">Unsere Events</h1>

<div class="activities">
  <div class="gray">
    <div class="date-time">fr<br>17:00</div>
    <h1>Smallgroup</h1>
    <div class="description">Zeit mit deinen Freunden</div>
    <!--this should be link with id, not to new membership.-->
  </div>
  <div class="gray">
    <div class="date-time">sa<br>7:30</div>
    <h1>Packen</h1>
    <div class="description">Alle Gepäcksstücke müssen um 7:30 bei der Mensa bereitstehen</div>
    <!--this should be link with id, not to new membership.-->
  </div>
  <div class="gray">
    <div class="date-time">sa<br>9:30</div>
    <h1>Aufräumen</h1>
    <div class="description">Putzen ist angesagt. Was genau deine Aufgabe ist, findest du bei deinen Angeboten. Sonst wende dich bitte an deinen Smallgroup Leiter. Sonst wende dich bitte an deinen Smallgroup Leiter.</div>
    <!--this should be link with id, not to new membership.-->
  </div>
</div>








<!--
<h1>Listing users</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3">actions</th>
    </tr>
  </thead>

  <tbody class="table">
    <% @users.each do |user| %>

      <tr>

        <td><a href="users/<%= user.id %>"><%= user.firstname %> <%= user.name %> <i class="fa fa-external-link"></i></a></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user %></td>
        <td><a href="<%= edit_user_path(user) %>">edit <i class="fa fa-pencil-square-o"></i></a></td>
        <td><%= link_to 'Destroy', user, method: :destroy, data: { confirm: 'Are you sure?' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h2>Import Products</h2>

<%= form_tag import_users_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import CSV" %>
<% end %>

<br>

<%= link_to 'New User', new_user_path, {:class => "button"} %>
-->